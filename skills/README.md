# 技能库

用于存放可被 AI 复用的技能定义文件。

建议约定：

- 每个技能一个独立目录。
- 每个技能目录至少包含一个 `SKILL.md`。
- `SKILL.md` 负责说明适用场景、执行步骤、输出格式和质量检查项。
- 目录名可以使用中文，`SKILL.md` 里的 `name` 建议使用英文小写和连字符。

当前技能：

- `需求分析/`
- `市场调研/`
- `PRD写作/`
- `评审检查/`
- `提示词优化/`
- `交互页面生成/`
- `问题答疑/`
- `规则逻辑速查/`
